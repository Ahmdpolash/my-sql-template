<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Backend Template || Server</title>
    <style>
      body {
        margin: 0;
        font-family: Arial, Helvetica, sans-serif;
        background: linear-gradient(135deg, #7b2cfa, #cc72f5, #ff9a9e);
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        animation: gradientMove 10s ease infinite;
        background-size: 300% 300%;
      }

      @keyframes gradientMove {
        0% {
          background-position: 0% 50%;
        }
        50% {
          background-position: 100% 50%;
        }
        100% {
          background-position: 0% 50%;
        }
      }

      .card {
        background: rgba(255, 255, 255, 0.15);
        backdrop-filter: blur(10px);
        box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
        border-radius: 20px;
        width: 90%;
        max-width: 600px;
        padding: 40px;
        text-align: center;
        color: #fff;
        transform: translateY(20px);
        animation: slideUp 1s ease forwards;
      }

      @keyframes slideUp {
        to {
          transform: translateY(0);
          opacity: 1;
        }
      }

      h1 {
        font-size: 2.5rem;
        margin: 0 0 10px;
        letter-spacing: 1px;
      }

      p {
        margin: 0;
        font-size: 1.2rem;
        opacity: 0.9;
      }

      .btn {
        margin-top: 20px;
        padding: 12px 30px;
        background-color: #ffffff;
        color: #7b2cfa;
        border: none;
        outline: none;
        border-radius: 30px;
        cursor: pointer;
        font-size: 1rem;
        font-weight: bold;
        transition: all 0.3s ease;
      }

      .btn:hover {
        background-color: #7b2cfa;
        color: #fff;
        transform: scale(1.05);
      }
    </style>
  </head>
  <body>
    <div class="card">
      <h1>🚀Welcome to Backend Server</h1>
      <p>Your backend is running smoothly!</p>
      <button class="btn" onclick="window.location.href='/health'">Check Health</button>
    </div>
  </body>
</html>
